import 'package:flutter/material.dart';

/// Global navigator key used to navigate from outside the widget tree (e.g. providers).
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
